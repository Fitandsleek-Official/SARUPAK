# SARUPAK web — Phase 5.2 stable ports
# API must be sarupak-api on :4003 (not Norng Downloader on :4000).
# Restart `next dev` after changing this file — Next inlines NEXT_PUBLIC_* at startup.
NEXT_PUBLIC_API_URL=http://localhost:4003/v1
NEXT_PUBLIC_WORKER_URL=
